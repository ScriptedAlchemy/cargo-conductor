# Cargo-hauler host swap tuning

Audit and safe-subset application performed on 2026-09-02 for
`ubuntu-main`, an Ubuntu 24.04 host with 96 CPUs, 92,574,900 KiB
(88.3 GiB) RAM, and kernel `7.0.0-28-generic`.

No memory load test, swapoff, or live zram resize was performed.

## Before state

| Area | Before value | Finding |
| --- | --- | --- |
| Swap | zram: 24 GiB, 13.7 GiB used, priority 100; disk file: 24 GiB, 4.54 GiB used, priority -1 | Current total use was 18.3 GiB. The disk tier exceeded the 4 GiB safety threshold, so its live priority was not changed. Historical incidents reached roughly 36 GiB used and 100% swap occupancy. |
| Zram source | `/etc/systemd/zram-generator.conf`, generated by `systemd-zram-generator` 1.1.2 | The main file used `min(ram / 2, 24576)`, `zstd`, and priority 100. |
| Zram compression | `orig_data_size=14,523,887,616`, `compr_data_size=3,296,329,218`, `mem_used_total=3,410,919,424` bytes | Payload compression was 4.40:1 and effective compression including allocator overhead was 4.26:1. This clears the requested 2.5:1 threshold for a 50%-of-RAM reboot target. |
| MGLRU | `/sys/kernel/mm/lru_gen/enabled = 0x0007` | All exposed MGLRU feature bits were enabled. |
| VM sysctls | `swappiness=150`, `watermark_scale_factor=10`, `min_free_kbytes=67584`, `page-cluster=0` | Swappiness was persisted by `/etc/sysctl.d/90-interactive-dev-vm.conf`; page clustering was already disabled live but was not in the merged persistent configuration. |
| Userspace OOM handling | `systemd-oomd` and `earlyoom` both absent | Ubuntu packages are available, but neither service nor an `oomd.conf` existed. |
| TraceDecay cgroup | User unit, inactive; `MemoryHigh=26 GiB`, `MemoryMax=30 GiB`, `MemorySwapMax=infinity`, `OOMScoreAdjust=200` | There is no system `tracedecay.service`. The user manager delegates the memory controller and has accounting enabled. Because the unit was inactive, its leaf cgroup did not exist during this audit. Its last recorded resident peak was 26.0 GiB, below the 30 GiB cap, while swap peaked at 18.1 GiB and was unlimited. This explains why global OOM could arrive before `CONSTRAINT_MEMCG`; missing delegation is not the leading diagnosis. |
| Parent user cgroup | memory controller delegated; `memory.events` included `oom_kill 103` | The controller is operational at `/user.slice/user-1000.slice/user@1000.service`; the evidence does not support an accounting failure. |
| Storage topology | `/var/tmp` on root ext4 `/dev/sda1`; `/fast` on `fastpool/fast` ZFS | The emergency swapfile is on a separate virtual disk from the build pool. Both are advertised non-rotational, but disk swap still introduces storage latency. |
| Disk swap persistence | None | The file was created on 2026-08-30, had no `fstab` entry or unit fragment, and was active as a discovered swap unit. |

The prior OOM forensics also found 863 kernel OOM kills in a month,
`active_file:0`, and global rather than memcg OOMs. Those historical values
were accepted as incident evidence and were not reproduced.

## Applied safe subset

### Live and persistent sysctls

`/etc/sysctl.d/99-hauler-swap-tuning.conf` now contains:

```ini
vm.page-cluster = 0
vm.watermark_scale_factor = 125
vm.swappiness = 100
```

All three values were applied live and read back. `page-cluster=0` was a live
no-op but is now explicit across reboot. The higher watermark makes `kswapd`
begin background reclaim earlier; swappiness 100 remains zram-friendly while
reducing aggressive anonymous-memory spill into the disk tier.

Revert:

```bash
sudo rm /etc/sysctl.d/99-hauler-swap-tuning.conf
sudo sysctl -w vm.page-cluster=0 vm.watermark_scale_factor=10 vm.swappiness=150
```

### Zram next-boot sizing

`/etc/systemd/zram-generator.conf.d/99-hauler-swap-tuning.conf` overrides the
main generator file with:

```ini
[zram0]
zram-size = ram / 2
compression-algorithm = zstd
swap-priority = 100
```

This targets approximately 44 GiB on this host. It is persistent but was not
applied live; resizing an active zram swap device would violate the no-mass-
swapoff constraint.

Revert:

```bash
sudo rm /etc/systemd/zram-generator.conf.d/99-hauler-swap-tuning.conf
```

The existing main generator file then restores the 24 GiB cap on the next
boot.

### Emergency disk priority next activation

`/etc/fstab` now contains:

```fstab
/var/tmp/td-toolprof-swapfile none swap sw,pri=-2 0 0
```

The generated swap unit points to this entry and contains `pri=-2`. The active
kernel priority remains -1 because 4.54 GiB was resident in the disk swapfile,
above the 4 GiB cutoff. Priority -2 takes effect on the next activation or
reboot; no swapoff occurred.

Revert persistence without touching the active swap device:

```bash
sudo sed -i '\|^/var/tmp/td-toolprof-swapfile none swap sw,pri=-2 0 0$|d' /etc/fstab
sudo systemctl daemon-reload
```

## Staged recommendations requiring sign-off

Nothing in this section was installed, enabled, or restarted. Candidate files
are under `/var/tmp/cargo-hauler-host-tuning-staged/`.

### systemd-oomd candidate

The staged systemd-native choice is:

- `oomd/90-hauler-pressure.conf`: global 60% memory-pressure threshold held
  for 30 seconds.
- `oomd/90-hauler-user-slices.conf`: opts per-user slices into
  `ManagedOOMMemoryPressure=kill`.
- `install-candidate.sh oomd`: installs the Ubuntu `systemd-oomd` package
  while runtime-masked, installs both policies, then explicitly enables it.

This integrates with cgroup v2 and PSI and kills a descendant cgroup rather
than selecting a process by regex. `earlyoom` is simpler and more predictable
when process-name control is paramount: a candidate policy would prefer
`^(rustc|rg|node)$` and avoid `^(cursor|sshd|tracedecay)$`. It polls
free-memory and swap thresholds rather than using PSI, so systemd-oomd is the
staged default. Enabling either policy changes kill behavior and needs
operator sign-off.

### TraceDecay candidate

`tracedecay/90-host-memory-guard.conf` proposes:

```ini
[Service]
MemoryAccounting=yes
MemoryHigh=22G
MemoryMax=26G
MemorySwapMax=8G
OOMScoreAdjust=0
```

This starts throttling below the old peak, creates a finite resident and swap
envelope, and removes the explicit global-OOM victim bias. The install script
updates the authoritative `systemctl --user set-property` memory controls and
installs the reviewable drop-in, but deliberately does not restart the
service. Product-owner sign-off and a maintenance window are required.

## Interaction with cargo-hauler admission

Cargo-hauler remains the first control plane, with memory-PSI soft and hard
admission thresholds of 10 and 20. Earlier background reclaim, less aggressive
anonymous swapping, a larger high-priority compressed tier, and emergency-only
disk swap should reduce steady-state swap-in/swap-out stalls. That should keep
memory PSI below the daemon's soft clamp more often and reduce hard-clamp
frequency.

The staged oomd threshold is a later, sustained-pressure backstop, not a
replacement for admission control. If enabled, compare hauler clamp counts,
`/proc/pressure/memory`, swap I/O, and oomd kill logs; do not infer equivalence
between hauler's short admission thresholds and oomd's 60%/30-second policy.

## Verification performed

- Read back `page-cluster=0`, `watermark_scale_factor=125`, and
  `swappiness=100`; `min_free_kbytes` remained 67584.
- Verified `/etc/fstab` had one swapfile entry and zero parse errors.
- Reloaded the system manager; the generated swap unit now has
  `SourcePath=/etc/fstab` and `Options=sw,pri=-2`.
- Confirmed `swapon --show --bytes` still reports the active disk tier at
  priority -1, proving it was not cycled.
- Parse-checked the staged systemd unit directives and shell syntax without
  applying the candidates.
- Ran no memory stress or load tests.

After reboot, verify with:

```bash
swapon --show --bytes
zramctl
sysctl vm.page-cluster vm.watermark_scale_factor vm.swappiness
```
