# Changesets

Run `pnpm changeset` for user-visible changes and commit the generated Markdown
file with the change. Merging to `master` updates the release pull request;
merging that pull request publishes the package through npm trusted publishing.
